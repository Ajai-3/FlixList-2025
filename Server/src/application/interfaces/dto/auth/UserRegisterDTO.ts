export interface UserRegisterDTO {
  name: string;
  email: string;
  username: string;
  password: string;
}

export interface UserResponseDTO {
  id: string;
  name: string;
  username: string;
}
